.header
  = render 'shared/header-left'
  = render 'shared/header-right'

= render 'shared/side-bar'

.chat-main
  = render partial: 'message-main', collection: @messages, as: :message
  #add-auto-update-message
= render 'message-form'
